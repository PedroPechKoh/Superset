{
  "build": {
    "builder": "DOCKERFILE",
    "watchPatterns": ["Dockerfile", "superset_config.py"]
  },
  "deploy": {
    "startCommand": "gunicorn -w 2 --timeout 120 -b 0.0.0.0:8088 'superset.app:create_app()'"
  }
}